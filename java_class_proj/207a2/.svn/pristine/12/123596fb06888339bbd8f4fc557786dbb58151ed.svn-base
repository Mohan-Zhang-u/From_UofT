package Commands;

public class Mkdir extends Command{
  
  private String[] commandKeywords;
  
  public Mkdir(String input){
    super(input);
  }
  
  public void execute(){
    super.execute();
    /** find if keywords satisfy the format
     * 
     */
    if(commandKeywords.length == 2 && commandKeywords[0] == "mkdir"){
      
    }
  }
}
